# Changelog

## 0.4.2

- 移除手动输出像素设置，按 Esri 第 19 级瓦片与区域纬度自动计算可用的最高标称分辨率，并显示 m/px、像素面积和实际输出尺寸。
- Agnes 2.5 保持双图理解请求；区域 PNG 在发送前生成受控 JPEG 传输副本，规避 Vercel 请求体限制导致的秒级失败。
- 历史档案查询以 512 m 为界：不超过 512 m 时逐瓦片完整扫描；更大范围按 3 × 3、4 × 4、5 × 5 代表网格自适应扫描，最多 25 个位置。

## 0.4.1

- 支持自定义区域地面范围与输出像素，并动态显示每像素地面面积。
- 区域几何、影像裁切、地图预览、导出元数据和界面规格均改为按区域参数计算。

## 0.4.0

- 项目更名为 TerraChron ChangeLens。
- 重写项目 README，并新增甲方本地模型客户端保姆式接入教程。

## 0.3.3

- Agnes 变化检测同时传递带身份的历史影像与最新影像，并以最新 POI 作为当前参考。
- 历史 POI 增加可选注入接口，未找到或失败时不传递。
- 为 02、03 拖拽窗口保留布局占位，避免首次拖拽后窗口重排。

## 0.3.2

- 修复拖拽区域影像库或模型浮窗时被拉伸至整屏的问题。
- 恢复本地模型地址优先、Agnes 代理兜底的模型客户端选择。

## 0.3.1

- 增加与区域影像库联动的 03 模型输入与输出浮窗。
- 通过 Vercel 服务端 Agnes 代理安全传递历史图、POI、元数据和提示词。

## 0.3.0 — 2026-09-10

- 增加本地模型变化检测的传输、区域状态、结果适配和渲染链路。

## 0.2.0 — 2026-09-09

- 区域库改为右侧固定宽度浮窗；新增区域只在列表内部滚动，不再撑开面板。
- 每个区域在截取完成后通过 Vercel 服务端函数请求高德 POI 描述，Key 仅保存在 `AMAP_API_KEY` 环境变量。
- POI 摘要使用单行省略；悬停时通过页面顶层带阴影的临时浮层显示完整内容。
- 历史查询从全局时间范围改为区域卡片内的单一时间点：年份必选，月份和日期可选，返回拍摄日期最近的一张历史影像。
- 增加高德 Key 类型不匹配的可读提示。
- 新增时间点、POI、区域状态和浏览器交互测试。

## 0.1.0 — 2026-09-09

- 首次发布：WGS84 区域截取、Esri Wayback 历史查询、影像比对、ZIP 导出和 HTTP 传递。
