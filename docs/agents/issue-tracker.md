# 工作追踪

本项目为无远程仓库的独立本地项目。以 Git 提交、版本标签和本文件追踪工作，不创建外部 issue。

## 工作记录 3 · 2026-09-10 · 本地模型接入预留

**状态：已完成前端预留，等待甲方本地模型服务。**

- 按 V1.0 文档封装健康检查、模型信息和 `/analyze` 的 multipart 传输。
- 使用历史影像、当前 POI JSON、WGS84 bbox 与历史日期构造模型输入。
- 增加区域级请求状态、取消处理、Markdown/JSON 渲染和显式变化结论适配。
- 未设置 `VITE_MODEL_API_BASE` 时不发起网络调用；模型按钮以配置提示保持禁用。
- 已提供 `createTerraChronApi(pinia, { modelClient })` 注入点，供甲方本地桥接替换传输实现。

## 工作记录 2 · 2026-09-09 · v0.2.0

**状态：已完成并发布。** 生产地址为 https://terrachron.vercel.app/ ，部署 ID 为 `dpl_4Zm4Q9kSPkXg847qhNuqKvneX5mQ`。

- 将历史时间选择、查询和最近影像预览移入各区域卡片。
- 增加高德 Web 服务 POI 查询，API Key 只读取 Vercel 的 `AMAP_API_KEY`。
- 将区域影像库固定在右侧并限制宽度；POI 全文通过页面顶层带阴影浮层显示。
- 验证：21 个 Node 单元测试、2 个 Playwright 浏览器流程、Vite 生产构建与生产主页 HTTP 200。
- 可回退提交：`fa760c4`；发布版本标签：`v0.2.0`。

## 工作记录 1 · 2026-09-09 · v0.1.0

**状态：已完成。** 建立独立项目及仓库，完成区域截取、Esri Wayback 历史影像、比对和导出基础功能。

## 工作记录 4（2026-09-10）

- 在区域影像库下方增加可拖拽的 03 / MODEL 浮窗，并随当前选择的区域同步模型输入。
- 新增 /api/agnes 服务端代理，读取 AGNES_API_KEY，不会向前端暴露密钥。
- 使用结构化提示词模拟变化检测，结果在同一浮窗中渲染。

## 工作记录 5（2026-09-10）

- Agnes 链路改为历史影像与最新影像双图对比，服务端提示词标明图像身份和时间。
- 为甲方预留 historyPoiLookup；无可信历史 POI 时严格省略字段。
- 02 与 03 拖动时保留独立布局槽位，避免后续窗口顶替原位置。

## 工作记录 6（2026-09-10）

- 项目命名为 TerraChron ChangeLens，完善对外 README。
- 新增甲方本地模型客户端教程，覆盖环境变量、CORS、multipart 契约、结果格式、历史 POI 注入和桌面桥接。
- 准备创建并推送个人 GitHub 远程仓库。

## 工作记录 7（2026-09-11）

- 将固定 256 m × 256 m、512 px 截取改为区域级自定义范围与像素。
- 新增每像素地面面积计算；区域裁切、地图范围、历史预览和导出元数据按参数动态计算。
